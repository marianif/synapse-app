# Plan

The build spine: macro-areas of the product, each a set of tasks, ranked by GOALS.md.

## Area — Capture
- Serves: Tier 1 (Capture a thought)
- [ ] `optimize use-shared-intake` — foreground Synapse via the unused synapseapp scheme when content is shared in. @week effort: M
- [ ] `clarify diary-filter-bar` — replace the bare filter glyph with a real idea-browsing affordance on Notes. @week effort: S

## Area — Detail & editing
- Serves: Tier 1 (Glanceable presence) / Tier 2 (board grooming)
- [ ] `harden edit.tsx` — finish and commit the view/edit split; verify DateInput/TimeInput changes don't regress the detail form. effort: M
- [ ] `polish direct-detail-sheet` — visual pass now that DetailHero is gone and metadata renders as structured rows. @week effort: S
- [ ] `flaws direct-detail-sheet` — known bugs in the project detail modal. effort: S
